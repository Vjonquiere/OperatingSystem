#ifdef CHANGED
#include "syscall.h"
int main(){
    ForkExec("test/threadloopprocess");
    ForkExec("test/threadloopprocess");
    ForkExec("test/threadloopprocess");
    ForkExec("test/threadloopprocess");
    ForkExec("test/threadloopprocess");
}
#endif